CONNECT HYDROQUEBEC1/Azerty77;
DROP TABLE EMPLOYES_CATEGORIES;
/
DROP TABLE EMPLOYES;

/
CREATE TABLE EMPLOYES(
    ID_EMP INT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,
    NOM VARCHAR2(255) NOT NULL,
    SALAIRE NUMBER(7,2) NOT NULL,
    DEPARTEMENT VARCHAR2(100) NOT NULL
);
/
INSERT INTO EMPLOYES(NOM, SALAIRE, DEPARTEMENT) VALUES ('Alice', 2500, 'IT');
INSERT INTO EMPLOYES(NOM, SALAIRE, DEPARTEMENT) VALUES ('Bob', 3200, 'MARKETING');
INSERT INTO EMPLOYES(NOM, SALAIRE, DEPARTEMENT) VALUES ('Carla', 2800, 'FINANCE');
INSERT INTO EMPLOYES(NOM, SALAIRE, DEPARTEMENT) VALUES ('Awa', 4500, 'IT');
INSERT INTO EMPLOYES(NOM, SALAIRE, DEPARTEMENT) VALUES ('David', 2900, 'MARKETING');
INSERT INTO EMPLOYES(NOM, SALAIRE, DEPARTEMENT) VALUES ('Sara', 5000, 'IT');
INSERT INTO EMPLOYES(NOM, SALAIRE, DEPARTEMENT) VALUES ('Frank', 2700, 'FINANCE');
INSERT INTO EMPLOYES(NOM, SALAIRE, DEPARTEMENT) VALUES ('Ali', 6000, 'FINANCE');
INSERT INTO EMPLOYES(NOM, SALAIRE, DEPARTEMENT) VALUES ('Marwa', 3100, 'MARKETING');
INSERT INTO EMPLOYES(NOM, SALAIRE, DEPARTEMENT) VALUES ('Aboubacar', 2400, 'IT');
-- /
-- SET SERVEROUTPUT ON;
-- /
-- -- UPDATE EMPLOYES SET SALAIRE = SALAIRE *1.1;
-- -- COMMIT;
-- /
-- -- Exercice_3 sur la boucle FOR
-- DECLARE
--     V_SALAIRE EMPLOYES.SALAIRE%TYPE;
--     C_BASE_LEVEL CONSTANT EMPLOYES.SALAIRE%TYPE := 3000;
-- BEGIN
--     FOR EMP IN (SELECT * FROM EMPLOYES) LOOP
--         V_SALAIRE := EMP.SALAIRE;
--         IF V_SALAIRE < C_BASE_LEVEL THEN
--             DBMS_OUTPUT.PUT_LINE('Employe ID: ' || EMP.ID_EMP || ' - Salaire Avant Maj: ' || V_SALAIRE);
--             V_SALAIRE := V_SALAIRE * 1.1;
--             UPDATE EMPLOYES SET SALAIRE = V_SALAIRE WHERE ID_EMP = EMP.ID_EMP;
--             DBMS_OUTPUT.PUT_LINE('Employe ID: ' || EMP.ID_EMP || ' - Nouveau Salaire: ' || V_SALAIRE);
--         END IF;

--     END LOOP;
--     COMMIT;

-- END;
-- /
-- --Ex_4 
-- -- UPDATE EMPLOYES SET SALAIRE = SALAIRE +518 WHERE ID_EMP IN (7,8,9,10);
-- -- COMMIT;
-- DECLARE
--     V_SALAIRE EMPLOYES.SALAIRE%TYPE;
--     C_AUGMENTATION CONSTANT EMPLOYES.SALAIRE%TYPE := 518;
-- BEGIN
--     FOR ID IN 7..10 LOOP
--         SELECT SALAIRE INTO V_SALAIRE FROM EMPLOYES WHERE ID_EMP = ID;
--         DBMS_OUTPUT.PUT_LINE('Employe ID: ' || ID || ' - Salaire Avant Maj: ' || V_SALAIRE);
--         V_SALAIRE := V_SALAIRE + C_AUGMENTATION;
--         UPDATE EMPLOYES SET SALAIRE = V_SALAIRE WHERE ID_EMP = ID;
--         DBMS_OUTPUT.PUT_LINE('Employe ID: ' || ID || ' - Nouveau Salaire: ' || V_SALAIRE);
--     END LOOP;
--     COMMIT;

-- END;
-- /
-- --EX5
-- DROP TABLE EMPLOYES_CATEGORIES;
-- CREATE TABLE EMPLOYES_CATEGORIES(
--     ID INT  PRIMARY KEY,
--     NOM_EMPLOYE VARCHAR2(255) NOT NULL,
--     CATEGORIE VARCHAR2(255) NOT NULL,
--     CONSTRAINT FK_EMPLOYE FOREIGN KEY (ID) REFERENCES EMPLOYES(ID_EMP)
-- );

-- /
-- -- EX6
-- -- INSERT INTO EMPLOYES_CATEGORIES (ID, NOM_EMPLOYE, CATEGORIE)
-- -- SELECT
-- --     E.ID_EMP,
-- --     E.NOM,
-- --     CASE
-- --         WHEN E.SALAIRE < 3000 THEN 'JUNIOR'
-- --         WHEN E.SALAIRE >= 3000 AND E.SALAIRE <= 7000 THEN 'INTERMEDIAIRE'
-- --         ELSE 'SENIOR'
-- --     END AS CATEGORIE
-- -- FROM EMPLOYES E;

-- -- COMMIT;
-- DECLARE
--     V_SALAIRE EMPLOYES.SALAIRE%TYPE;
--     V_CATEGORIE_EMPLOYE EMPLOYES_CATEGORIES.CATEGORIE%TYPE;
-- BEGIN
    
--     FOR EMP IN (SELECT * FROM EMPLOYES) LOOP
--         V_SALAIRE := EMP.SALAIRE;
--         IF V_SALAIRE < 3000 THEN
--             V_CATEGORIE_EMPLOYE := 'JUNIOR';
--         ELSIF V_SALAIRE >=3000 AND V_SALAIRE <= 7000 THEN
--             V_CATEGORIE_EMPLOYE := 'INTERMEDIAIRE';
--         ELSE 
--             V_CATEGORIE_EMPLOYE := 'SENIOR';
--         END IF;
--         INSERT INTO EMPLOYES_CATEGORIES(ID, NOM_EMPLOYE, CATEGORIE) 
--         VALUES (EMP.ID_EMP, EMP.NOM, V_CATEGORIE_EMPLOYE);
--     END LOOP;
--     COMMIT;
-- END;
-- /