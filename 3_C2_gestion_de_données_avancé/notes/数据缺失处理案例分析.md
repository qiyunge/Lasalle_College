这个例子非常适合做一次完整的 缺失值分析（Missing Value Analysis）。你这个其实就是经典贷款审批数据（Loan Prediction）。

先统计缺失率。

总样本：

N=614

计算：

列	缺失数	缺失率
Gender	13	2.1%
Married	3	0.5%
Dependents	15	2.4%
Self_Employed	32	5.2%
LoanAmount	22	3.6%
Loan_Amount_Term	14	2.3%
Credit_History	50	8.1%

整体属于：

低~中度缺失

一般不建议删样本。

1 Gender（性别）
601/614
缺失 13
类别变量

先问：

为什么缺？

可能：

MCAR

用户忘填。

或：

MAR

某些群体不愿公开。

检查：

df.groupby(
"Education"
)["Gender"]
.apply(
lambda s:
s.isna().mean()
)

看看是否相关。

建议：

先：

df["Gender"].value_counts(
normalize=True
)

假设：

Male 80%
Female 20%

补：

fillna(
mode
)

或者：

Unknown

影响：

如果补：

全部 Male

可能：

模型：

放大男性比例

风险：

低。

2 Married（婚姻）
611
缺失3

这个你刚刚就在分析。

可能：

MNAR

未填写 ≠ 随机

可能：

未婚

检查：

df[
df["Married"]
.isna()
]

观察：

CoapplicantIncome
Dependents

例如：

共同收入：

0

可能：

未婚。

推荐：

规则补：

mask = (
df["Married"]
.isna()
)

df.loc[
mask,
"Married"
]=(
df.loc[
mask,
"CoapplicantIncome"
]
.gt(0)
.replace({
True:"Yes",
False:"No"
})
)

影响：

婚姻状态可能影响贷款。

中等。

3 Dependents（家属）
599
缺失15

可能：

MNAR

没填：

可能：

0

建议先看：

value_counts()

如果：

0最多

可：

fillna(
mode
)

或：

Unknown

影响：

家属影响负债能力。

中。

4 Self_Employed（自雇）
582
缺失32
5%

危险一点。

可能：

MAR

收入越高越不填。

检查：

groupby(
"Self_Employed"
)
[
"ApplicantIncome"
]
.mean()

建议：

条件补：

groupby(
"Education"
)
.transform(
mode
)

或者：

Unknown

影响：

收入稳定性。

较高。

5 LoanAmount（贷款金额）
592
缺失22

数值。

先：

df["LoanAmount"].hist()

大概率：

右偏。

推荐：

median

高级：

groupby(
Income
)

补。

例如：

df["LoanAmount"] = (
df["LoanAmount"]
.fillna(
df[
"LoanAmount"
]
.median()
)
)

影响：

非常高。

这是核心特征。

6 Loan_Amount_Term
600

期限。

检查：

value_counts()

通常：

360

最多。

推荐：

mode()

影响：

中。

7 Credit_History（最重要）
564
缺失50
8%

这个危险。

因为：

信用记录

通常：

MNAR

没记录 ≠ 随机。

可能：

首次贷款

推荐：

不要简单：

fillna(mean)

建议：

方案A：

fillna(0)

方案B：

增加：

Credit_missing

再：

fillna(
mode
)

例：

df[
"Credit_missing"
]=(
df[
"Credit_History"
]
.isna()
)

df[
"Credit_History"
]=(
df[
"Credit_History"
]
.fillna(0)
)

影响：

极高。

通常这是贷款预测最强变量。

不建议处理

删除：

Loan_ID

因为：

主键

转 category：

cat_cols = [
"Gender",
"Married",
"Dependents",
"Education",
"Self_Employed",
"Property_Area"
]

df[
cat_cols
]=(
df[
cat_cols
]
.astype(
"category"
)
)

最终方案（工程版）

Loan_ID
↓
drop

Gender
↓
mode

Married
↓
规则

Dependents
↓
mode

Self_Employed
↓
Unknown

LoanAmount
↓
median

Loan_Amount_Term
↓
mode

Credit_History
↓
missing_flag+0

这个思路已经接近真实 ML feature engineering，而不是单纯 fillna(mean)。