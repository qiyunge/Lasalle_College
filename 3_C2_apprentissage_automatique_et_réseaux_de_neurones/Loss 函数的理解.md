一、函数（function）

和

二、函数在某一点的值（function value）
三、真正固定的是谁

神经网络训练时：

真正固定的是：

L(θ)

这是：

一个定义好的数学函数。

例如：

假设：

网络：

y
^
	​

=f(x;θ)

loss：

MSE：

L(θ)=
N
1
	​

∑
i=1
N
	​

(f(x
i
	​

;θ)−y
i
	​

)
2

这里：

数据集：
(xᵢ, yᵢ)

固定。

网络结构：

固定。

因此：

整个：

L(θ)

已经完全确定。

四、什么在变化？

变化的是：

参数 θ 的取值

即：

你在 loss surface 上的位置。

五、类比：地形地图

想象：

loss landscape：

是一座山。

山本身：

固定。

不会因为你走一步：

山就改变。

但：

你的位置变了。

于是：

你看到的：

局部坡度：

变了。

因此：

函数没变
局部导数变了
六、为什么梯度会变化

梯度：

本质：

是：

函数在当前点的局部切平面。

例如：

函数：

f(x)=x
2

在：

x=-10：

梯度：

-20

在：

x=-1：

梯度：

-2

在：

x=0：

梯度：

0

函数没变。

但：

位置不同，

梯度不同。

七、为什么神经网络里更容易误解

因为：

神经网络：

不是简单：

f(x)

而是：

高维：

L(θ
1
	​

,θ
2
	​

,...,θ
n
	​

)

维度：

可能：

百万级。

因此：

每次更新参数后：

局部几何变化巨大。

于是：

直觉上像：

整个loss函数变了

实际上：

只是：

你移动到了：

另一个区域。

八、为什么“局部形状”会变化

虽然：

全局函数固定。

但：

不同区域：

曲率不同。

例如：

平坦区域
______

梯度很小。

陡峭区域
/|
 |

梯度很大。

Saddle Point
\_/
 /\

某方向上升，
某方向下降。

因此：

你移动后：

“看到的局部世界”
完全不同。

九、真正变化的是 Taylor Approximation

Gradient Descent：

每一步：

其实都在做：

局部线性近似：

L(θ+Δθ)≈L(θ)+∇L(θ)
T
Δθ

这里：

梯度：

只在：

当前点附近有效。

更新后：

必须：

重新估计局部近似。

因此：

看起来像：

“换了函数”。

十、为什么 SGD 更像“动态函数”

理论上：

完整 loss：

固定：

L(θ)=
N
1
	​

∑
i
	​

L
i
	​

(θ)

但：

SGD：

每次只看：

mini-batch：

L
B
	​

(θ)=
∣B∣
1
	​

∑
i∈B
	​

L
i
	​

(θ)

由于：

batch 不同。

所以：

你每次优化的：

其实是：

不同的近似 loss。

这时候：

“函数在变”

就不只是错觉了。

十一、真正完整的理解
Full Batch GD

函数：

固定。

只是：

位置变。

SGD / Mini-batch

连：

近似函数本身

都在变化。

因此：

优化轨迹：

更 noisy。

十二、为什么 SGD 还能工作

因为：

虽然：

每一步：

不是精确优化真实 loss。

但：

期望意义上：

E[∇L
B
	​

(θ)]≈∇L(θ)

即：

平均方向：

仍然正确。

十三、真正深层本质

训练神经网络：

本质：

不是：

在固定直线方向上下降

而是：

不断重新估计局部几何结构

所以：

Gradient Descent：

更像：

边观察边移动

而不是：

“已知地图”。

十四、最终一句话总结

严格数学上：

完整 loss function：

L(θ)

通常是固定的。

变化的：

是：

参数位置 θ

从而导致：

梯度变化
曲率变化
局部线性近似变化

而：

在 SGD 中，

由于 mini-batch 不同，

连：

局部近似目标函数

本身也在变化。