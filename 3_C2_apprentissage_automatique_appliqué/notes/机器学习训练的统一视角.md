机器学习训练的统一视角
Step 1：先决定世界怎么产生数据（概率假设）

你先假设：

P(y∣x,θ)

或者：

P(x,y∣θ)

这是模型假设（Model Assumption）

例子：

任务	假设
回归	y∼N(f(x),σ
2
)
分类	y∼Bernoulli(p)
多分类	y∼Categorical(p)
鲁棒回归	y∼Laplace

这一步已经引入：

Inductive Bias
	​

Step 2：决定如何选择参数（学习原则）

目标：

找：

θ

让训练数据最合理。

两种经典原则：

MLE

最大似然：

θ
∗
=argmaxP(D∣θ)

解释：

哪组参数最容易生成训练数据？

只相信数据。

MAP

最大后验：

θ
∗
=argmaxP(D∣θ)P(θ)

解释：

数据合理，同时参数别太离谱。

加入先验。

Step 3：把原则变成优化问题（Loss）

因为直接乘概率不好优化。

取负 log：

min−logP(D∣θ)

变成 loss。

统一推导链
概率假设
↓
Likelihood
↓
MLE/MAP
↓
Loss
↓
Gradient
↓
训练
情况1：线性回归

模型：

y=f(x)+ϵ

假设：

ϵ∼N(0,σ
2
)

↓

Likelihood：

P(y∣x)=e
−(y−f(x))
2

↓

MLE：

maxP(D∣θ)

↓

负 log：

MSE
	​


如果 MAP：

先验：

w∼N(0,τ
2
)

↓

得到：

MSE+λ∣∣w∣∣
2
	​


就是：

Ridge。

情况2：分类

模型：

P(y=1∣x)=σ(w
T
x)

假设：

y∣x∼Bernoulli

↓

Likelihood：

P=p
y
(1−p)
1−y

↓

MLE

↓

负 log：

Cross Entropy
	​


如果 MAP：

先验：

w∼N

↓

得到：

CrossEntropy+L2
	​

情况3：异常值很多

假设：

ϵ∼Laplace

↓

MLE

↓

得到：

MAE
	​


混合：

高斯+异常值：

↓

得到：

Huber
	​

最终统一表
数据假设	学习原则	Loss
Gaussian	MLE	MSE
Gaussian + Prior	MAP	MSE+L2
Laplace	MLE	MAE
Bernoulli	MLE	CE
Bernoulli + Prior	MAP	CE+L2
混合噪声	MLE	Huber
再往上抽象一层

训练一个模型，其实是在回答：

世界如何生成数据？
↓
什么参数最合理？
↓
如何度量合理？
↓
如何搜索？

对应：

Distribution
↓
MLE/MAP
↓
Loss
↓
Optimizer

这条链你后面学：

Logistic Regression
SVM
CNN
Transformer
Bayesian Learning